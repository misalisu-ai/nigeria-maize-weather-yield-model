[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nigeria-maize-weather-yield-model"
version = "0.1.0"
description = "Research models and distribution-shift experiments for Nigerian maize yield prediction."
requires-python = ">=3.10"
